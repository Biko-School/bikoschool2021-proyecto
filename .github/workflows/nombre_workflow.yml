name: PR Linters and Test
on:
  push:
    branches:
      - 'silvia-javier'
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
      - name: Instalar dependencias
        working-directory: front
        run: npm ci
      - name: Pasar prettier
        working-directory: front
        run: npm run prettier
      - name: Generar build
        working-directory: front
        run: npm run build
      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKEN: 1//03GqhjvgZWiDCCgYIARAAGAMSNwF-L9IrU8B8a14hiN7xgltvc5YkoRsAoVpTVIJzYK1wg423FNU2JQatInJKXY4INiuVrNPC8J8